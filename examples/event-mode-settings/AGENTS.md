# Event Mode Settings Agent Notes

Read Lynx docs before editing: https://lynxjs.org/llms.txt.

This example intentionally demonstrates warnings for `globalPropsMode: "event"` and lazy loading. Prefer `useGlobalPropsChanged` for reactive global-props updates and wrap lazy content in `Suspense`.
